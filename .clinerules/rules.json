{
  "project": "codereview-agent",
  "purpose": "Coding & contribution rules for this workspace",
  "version": "2.0",
  "generated": "auto",
  "project_type": "fastapi-api",
  "rules": {
    "do": [
      "Run `pytest` before committing; add tests for new features",
      "Use `Depends()` for dependency injection in route handlers",
      "Define Pydantic response models for all endpoints",
      "Store API keys in `.env` or environment variables — never hardcode (openai, perplexity)",
      "Add retry logic with exponential backoff for external API calls",
      "Validate and type-check API responses before using them",
      "Follow existing project structure and naming conventions",
      "Keep module imports at file top; use absolute imports within the project"
    ],
    "dont": [
      "Don't use sync functions for I/O in async route handlers",
      "Don't skip Pydantic validation by accessing `request.json()` directly",
      "Don't log or print full API responses in production (may contain PII)",
      "Don't ignore rate-limit headers from API providers",
      "Don't commit secrets, API keys, or `.env` files",
      "Don't add dependencies without checking license compatibility"
    ]
  },
  "priorities": [],
  "rules_count": 14,
  "architecture": [
    "**Project type**: fastapi-api",
    "**Entry points**: src\\main.py",
    "**Structural patterns**: fastapi-api, pytest-tests",
    "**Languages**: python"
  ],
  "file_structure": [
    "`src\\main.py`",
    "fastapi-api",
    "pytest-tests"
  ],
  "testing": [
    "**Framework**: pytest",
    "**Test files**: 1 (2 test cases)",
    "**Test types**: unit"
  ],
  "context_strategy": [
    "**src.main**: `src\\main.py` and its imports",
    "`**/*.pyc`",
    "`**/__pycache__/**`",
    "`**/.venv/**`",
    "`**/node_modules/**`",
    "`**/*-skills.md`",
    "`**/*-skills.json`",
    "`**/.clinerules*`"
  ],
  "auto-trigger": [
    "After design approval (DESIGN.md exists)",
    "User says: \"Let's implement this\", \"Create a plan\"",
    "Before starting implementation"
  ],
  "pre-review_checklist": [
    "✓ Read every line you changed",
    "✓ Remove debug statements",
    "✓ Check for commented-out code",
    "✓ Verify no secrets/credentials",
    "✓ All tests pass locally",
    "✓ New tests for new features",
    "✓ Edge cases covered",
    "✓ No skipped/ignored tests without reason",
    "✓ Follows project style guide",
    "✓ Functions < 50 lines",
    "✓ No TODO/FIXME without ticket reference",
    "✓ Docstrings for public APIs",
    "✓ README updated if needed",
    "✓ API docs updated",
    "✓ CHANGELOG.md entry added",
    "✓ Comments explain \"why\", not \"what\"",
    "✓ No unnecessary dependencies added",
    "✓ If new deps: justify in PR description",
    "✓ Lock file updated",
    "✓ No obvious performance issues",
    "✓ Database queries optimized",
    "✓ No N+1 queries",
    "✓ No SQL injection vectors",
    "✓ User input sanitized",
    "✓ Authentication/authorization checked",
    "✓ No sensitive data in logs"
  ],
  "code_review_self-assessment": [
    "[✓] All tests pass",
    "[✓] No debug code",
    "[✓] Documentation updated",
    "[file] (+lines, -lines)"
  ],
  "5-phase_process": [
    "**Binary Search**: Comment out half the code",
    "**Trace Backwards**: From error to source",
    "**Instrumentation**: Log state before/after suspected lines",
    "Check assumptions",
    "Verify data types",
    "Review recent changes"
  ]
}